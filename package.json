{
  "name": "orc-camp",
  "version": "1.0.0",
  "description": "Local-first CLI dashboard that visualizes tmux sessions as camps and Claude Code / Codex agent sessions as orcs. Read-only discovery, status inference, and a localhost pixel dashboard.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "tmux",
    "ai-agent",
    "claude-code",
    "codex",
    "dashboard",
    "cli",
    "local-first",
    "orchestration",
    "developer-tools"
  ],
  "homepage": "https://github.com/hwangjongtaek/orc-camp#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hwangjongtaek/orc-camp.git"
  },
  "bugs": {
    "url": "https://github.com/hwangjongtaek/orc-camp/issues"
  },
  "bin": {
    "orc-camp": "bin/orc-camp.mjs"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "scan": "tsx src/main.ts scan",
    "serve": "tsx src/main.ts serve",
    "doctor": "tsx src/main.ts doctor",
    "build": "tsx scripts/build.mts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "test:all": "vitest run && vitest run --config vitest.e2e.config.ts",
    "measure:latency": "tsx scripts/measure-latency.mts",
    "smoke": "tsx scripts/smoke.mts",
    "test:watch": "vitest",
    "prepublishOnly": "npm run smoke"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
